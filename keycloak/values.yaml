keycloak:
  replicaCount: 3

  auth:
    adminPassword: "kcpass"

  postgresql:
    enabled: false
  
  externalDatabase:
    host: postgres-postgresql-ha-pgpool.default.svc.cluster.local
    port: 5432
    user: "postgres"
    password: "pgpass"
    database: "postgres"
  
  extraEnvVars:
    - name: KEYCLOAK_EXTRA_ARGS
      value: "--import-realm"
    - name: KEYCLOAK_IMPORT
      values: /opt/keycloak/data/import/realm.json
  
  extraVolumeMounts:
    - name: config
      mountPath: "/opt/keycloak/data/import"
      readOnly: true
  extraVolumes:
    - name: config
      configMap:
        name: keycloak-realm
        items:
        - key: "realm.json"
          path: "realm.json"